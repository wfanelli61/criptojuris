'use client';

export default function DashboardPage() {
    return (
        <div>
            <h1 style={{ fontSize: '1.5rem', color: 'var(--color-primary-dark)', marginBottom: '1rem' }}>
                Panel de Control
            </h1>
            <p style={{ color: '#6B7280' }}>Bienvenido a tu panel de control.</p>
        </div>
    );
}
